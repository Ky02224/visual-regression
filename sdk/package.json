{
  "name": "visual-regression-playwright",
  "version": "1.0.0",
  "description": "Playwright SDK for Visual Regression Workbench — drop-in Percy alternative",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch"
  },
  "keywords": ["visual-regression", "playwright", "testing", "percy-alternative"],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.0",
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  },
  "peerDependencies": {
    "@playwright/test": ">=1.30.0"
  }
}
