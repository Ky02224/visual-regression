-r requirements.txt
# fastapi.testclient (Starlette's TestClient) is built on httpx and refuses to
# import without it. Needed to exercise the API surface in-process instead of
# booting a real server on a port for every test.
httpx>=0.27.0
pytest>=8.2.0
pytest-cov>=7.1.0
ruff>=0.6.0
watchdog>=2.1.0
